---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    snapshot.home.arpa/enabled: "true"
  name: lidarr-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
